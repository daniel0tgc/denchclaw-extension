// Empty stub for Node built-in modules that get pulled into client bundles
// by libraries like html-to-docx. Mirrors webpack's `resolve.fallback: { fs: false }`
// behavior for Turbopack, which needs an explicit alias target.
module.exports = {};
